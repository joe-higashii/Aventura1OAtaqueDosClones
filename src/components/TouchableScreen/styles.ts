import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titleStyle: {
    color: "#fff",
    fontSize: 35,
    fontWeight: "500",
    marginTop: 12,
  }
});

export default styles;